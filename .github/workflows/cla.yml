name: Contributor License Agreement
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

jobs:
  cla:
    uses: signalfx/splunk-common-android/.github/workflows/cla_template.yaml@develop
    secrets: inherit
