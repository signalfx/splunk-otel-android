<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".ApplicationLifecycleModuleInstaller"
            android:authorities="${applicationId}.ApplicationLifecycleModuleInstaller"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>