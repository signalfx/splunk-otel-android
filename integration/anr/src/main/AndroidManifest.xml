<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".AnrModuleInstaller"
            android:authorities="${applicationId}.AnrModuleInstaller"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
