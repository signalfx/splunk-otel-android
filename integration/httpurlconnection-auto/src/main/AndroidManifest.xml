<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".HttpURLModuleInstaller"
            android:authorities="${applicationId}.HttpURLModuleInstaller"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
