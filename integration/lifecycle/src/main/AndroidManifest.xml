<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".LifecycleInstaller"
            android:authorities="${applicationId}.lifecycle-integration-installer"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>