// Common modules
include ':common:otel',
        ':common:storage'

// Instrumentation modules
include ':instrumentation:runtime:crash',
        ':instrumentation:runtime:anr',
        ':instrumentation:runtime:startup',
        ':instrumentation:runtime:networkrequest:library',
        ':instrumentation:buildtime:plugin'

// Integration
include ':integration:agent:api',
        ':integration:agent:module',
        ':integration:agent:internal',
        ':integration:startup',
        ':integration:networkrequest',
        ':integration:anr',
        ':integration:crash',
        ':integration:interactions'

// Integration tests
// TODO disable it for now because of different artifactory include ':integration-run'

// Test App
include ':app'

// Main entry point
include ':agent'
